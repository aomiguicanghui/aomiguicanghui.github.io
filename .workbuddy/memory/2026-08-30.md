# 2026-08-30

## 全站 UI/UX 重设计（ADND2E扩展不全书）

- 重写 `assets/app.css`：羊皮纸噪点背景、墨木顶栏+烫金渐变底线、内嵌式搜索框（SVG 放大镜图标）、目录树极简 chevron + 金色激活指示条、典籍排版（居中标题+烫金饰纹、衬线正文 15.5px/1.85、书版细线表格）、5 套主题全部按新 token 体系润色。
- 重写 `assets/chargen.css`：车卡向导统一视觉语言，步骤胶囊加 CSS 计数器序号（counter-reset:cgstep），卡片/按钮/选中态（✓ 标记）精修。
- `assets/app.js` 关键修复：
  - `sanitizeImported` 增加移除 `<style>` 标签 + 新函数 `normalizeContentStyles`：剥离 WinCHM/Word 内联噪声（font-family/size、margin、mso-* 等），纯黑文字色删除（暗色主题不再隐形），纯红 → `var(--c-accent)`，表格内联边框剥离（书版表格生效）。
  - `walkToc` 过滤目录源文件里的 "Loading table of contents..." 占位文本。
- 验证：agent-browser 截图验证 羊皮纸/翠林/秘蓝/黑曜 4 主题、搜索、车卡全流程、表格页，均正常。
- 注意：CSP 限制 `font-src 'self'`，不能用网络字体，字体栈只能用系统字体。
